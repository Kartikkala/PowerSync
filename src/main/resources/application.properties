spring.application.name=jwtSpring


spring.datasource.url= jdbc:postgresql://localhost:5432/ffmpeg_db
#  ${DB_URL}

spring.datasource.username=ffmpeg
#  ${DB_USERNAME}
spring.datasource.password=kartik12
#  ${DB_PASSWORD}

spring.datasource.driver-class-name=org.postgresql.Driver

server.port=8080

spring.jpa.hibernate.ddl-auto=update


spring.jpa.show-sql=true

jwt.secret = ${JWT_SECRET}

cashfree.app.id=${CASHFREE_APP_ID}
cashfree.secret.key=${CASHFREE_SECRET_KEY}
cashfree.env.url=https://sandbox.cashfree.com/pg/orders

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Provider 1: Google ---
# Google OAuth2 Credentials
# Standard Spring Boot OAuth2 Scopes
spring.security.oauth2.client.registration.google.client-id= ${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=${GOOGLE_CLIENT_SCOPE}

# --- Provider 2: GitHub ---
# spring.security.oauth2.client.registration.github.client-id=YOUR_GITHUB_ID
# spring.security.oauth2.client.registration.github.client-secret=YOUR_GITHUB_SECRET
 # spring.security.oauth2.client.registration.github.scope=user:email

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kamdarparas90@gmail.com
spring.mail.password=dkdujsjidewmavuk
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true






